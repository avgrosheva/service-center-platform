# Copy this file to .env.local (gitignored by Next.js's default .gitignore).
#
# Backend base URL, no trailing slash. Every request the app client makes
# is prefixed /api/v1 on top of this, matching the backend's own routing
# convention (see field-service-platform/README.md).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
